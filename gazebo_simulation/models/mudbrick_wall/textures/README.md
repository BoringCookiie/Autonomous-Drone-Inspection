# Gazebo Mudbrick Texture Assets

Place procedural crack overlay maps (`crack_morphology.png`), erosion depth textures (`erosion_texture.png`), and dampness overlay textures (`moisture_stain.png`) in this folder.
Used by Gazebo rendering engine for synthetic defect dataset generation and simulation testing.
